namespace GetDue.BuildingBlocks.Auth;

/// <summary>
/// Internal marker type used to root the assembly until the real JWT auth
/// handlers land in a follow-up PR.
/// </summary>
internal sealed class AssemblyMarker;
